# Password for protected pages (leave empty to disable protection)
SITE_PASSWORD=your-password-here

# Secret for signing auth tokens — change to a long random string
AUTH_SECRET=change-this-to-a-random-string

# Comma-separated paths to password-protect (default: /admin)
PROTECTED_PATHS=/admin
